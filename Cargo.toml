[package]
name = "open-weather-wizard"
version = "0.1.0"
edition = "2024"
authors = ["Arunkumar Mourougappane <amouroug@buffalo.edu>"]
description = "A simple and elegant desktop weather application written in Rust and iced.\nProvides current weather, forecasts, and animated weather icons packaged with the app.\nDesigned to be lightweight, extensible, and easy to localize or re-skin for different platforms and icon sets."
homepage = "https://github.com/arunkumar-mourougappane/open-weather-wizard"
license = "MIT"
[dependencies]
env_logger = "0.11.8"
iced = { version = "0.14.0", features = ["svg", "image", "tokio"] }
log = { version = "0.4.27", features = ["std"] }
reqwest = { version = "0.12.23", features = ["json"] }
rust-embed = { version = "8.7.2", features = ["compression"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.143"
tokio = { version = "1.47.1", features = ["full"] }
base64 = "0.22.1"
dirs = "6.0.0"
async-trait = "0.1.89"
anyhow = "1.0.100"
velato = { version = "0.10.0", features = ["wgpu"] }
wgpu = "27.0.1"

[[bin]]
name = "open-weather-wizard"
path = "src/main.rs"

[dev-dependencies]
tokio-test = "0.4.3"
